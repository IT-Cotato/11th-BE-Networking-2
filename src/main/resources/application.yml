spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cotato2
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: yujin0320

  jpa:
      hibernate:
        ddl-auto: create
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
          show_sql: true
          format_sql: true

  application:
    name: networking2